{
  "name": "ko-kbd",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "tsc && vitest run",
    "test:watch": "vitest watch",
    "build": "tsc && vite build",
    "preview:build": "tsc && vite build --mode preview",
    "preview:serve": "vite preview --mode preview"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.10",
    "vitest": "^1.0.4"
  }
}